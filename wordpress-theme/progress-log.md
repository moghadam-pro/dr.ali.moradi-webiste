# Progress Log

Running record of what has actually been built, in order. Each entry says
what changed and where, so this file plus `git log` on
`claude/wordpress-doctor-site-architecture-m4766g` is a complete record
without needing to replay the chat.

## 2026-09-02

- Architecture agreed (see `architecture.md`) and documentation branch
  (`docs`) created with this folder.
- Scaffolding started on `claude/wordpress-doctor-site-architecture-m4766g`:
  theme skeleton under `wordpress-theme/dr-ali-moradi/` (block theme:
  `theme.json`, template parts, page templates, custom post types, theme
  options page, dynamic blocks) and the content extraction script under
  `wordpress-theme/content-migration/`.
- Deployment to `tmp.saveon.me` and the bulk content import are pending
  site access — see `open-items.md`.
