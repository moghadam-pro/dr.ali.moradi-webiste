# Homepage v2 handoff context

This directory preserves the documentation state that accompanied the
`agent/homepage-figma-static-v2` handoff branch.

The canonical project documentation remains in `docs/`. Files here are kept as
reference evidence for the static Figma-aligned handoff and must not overwrite
newer canonical documents on `main`.

Source snapshot:

```text
agent/homepage-figma-static-v2
12d177635b3cef657faf5f471e12d575fef395cc
```
