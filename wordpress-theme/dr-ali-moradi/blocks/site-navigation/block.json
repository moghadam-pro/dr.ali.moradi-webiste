{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 3,
	"name": "dr-ali-moradi/site-navigation",
	"title": "Site Navigation (Menus screen)",
	"category": "dr-ali-moradi",
	"icon": "menu",
	"description": "Renders the menu assigned in Appearance -> Menus for the chosen location, so navigation is always managed on the classic Menus screen rather than inside the Site Editor.",
	"attributes": {
		"location": { "type": "string", "default": "primary" }
	},
	"supports": { "html": false },
	"render": "file:./render.php"
}
